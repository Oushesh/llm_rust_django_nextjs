##Test Script to showcase ingestion demo.

import os
import glob
from typing import List
from multiprocessing import Pool
from tqdm import tqdm


#TODO: Get load_dotenv() to load all variables

# Load Environment Variables
persist_directory =
